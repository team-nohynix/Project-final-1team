apiVersion: 1
datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    # 2026-08-25 홈서버 이전: CloudWatch 데이터소스 제거 — Kafka/MySQL/Redis가
    # 전부 Docker 컨테이너로 직접 도는 홈랩 환경이라 CloudWatch 자체가 없다.
